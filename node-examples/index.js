var rect = {
        perimeter: (x,y) => (2*(x+y)),
        area : (x,y) => (x*y)
};

function solverRect(l,b){
    console.log("Rectengle solve");

    if(l<=0){
        console.log("negative scenario");
    }
}

solverRect(4,5);
